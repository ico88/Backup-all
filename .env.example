# Copia questo file in .env e adattalo
PORT=8000
DEV=false

# Chiave di cifratura credenziali (generata automaticamente al primo avvio)
# Per produzione: genera con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# BACKUP_SECRET_KEY=...

# Percorso DB (default: SQLite locale)
# DATABASE_URL=sqlite:///./backup_all.db
